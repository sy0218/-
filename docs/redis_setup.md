# ⚡ Ubuntu에서 Redis 설치 & 환경 구축
---

## 📌 개요
- Ubuntu 환경에서 **Redis Docker 컨테이너 설치 및 기본 환경 구성** 가이드
- 데이터베이스 접근 및 초기 설정 방법 포함
- 로컬 설치 기준으로 작성 (Ansible 없이 직접 실행)

🚀 **Ansible로 자동화된 환경 설정 예시**는 🔗 [`Ansible 레포지토리`](https://github.com/sy0218/Ansible-Multi-Server-Setup)에서 확인하세요!

---
<br>

## ⚙️ Redis Docker 컨테이너 실행
```bash
docker run --name job_redis -d \
  -p 6379:6379 \
  -v /Data_project_job/docker_image/redis/rd_data:/data \
  redis:7 \
  redis-server \
  --requirepass 1234 \
  --appendonly no \
  --save 600 1
```
- `-v`: 호스트와 컨테이너 간 데이터 볼륨 매핑
- `-p 679:6379`: 로컬 포트 6379와 컨테이너 포트 6379 연결
- `--requirepass 1234`: Redis 비밀번호 설정
- `--appendonly no, --save 600 1`: RDB 방식 자동 스냅샷 설정

---
<br>

## ⚙️ Redis Docker 컨테이너 접속
```bash
docker exec -it job_redis redis-cli -a 1234
```

---
<br>

## ⚙️ 데이터베이스 선택 및 데이터 조회
```redis
# 데이터베이스 15번 선택
SELECT 15

# 예시 데이터 조회
HGET user:1 name
```

---
<br>

## ✅ 결과 확인
- Docker 기반 Redis 컨테이너 실행 완료
- Redis CLI 접속 가능
- 데이터베이스 선택 및 조회 확인 가능
- 로컬 서버 기준 캐시/세션/임시 데이터 환경 구축 가능
---
